#include<stdio.h>
int main()
{
    int x = 5, y;
    //y = ! x > 4;           // 1priorty ! then > then = so....!5--0--0>4--0
    //y =  x > 4 && x < 10;
    //y =  x > 4 || x < 10;
    printf("%d",y);
}